- name: Update all packages to their latest version
  apt:
    name: "*"
    state: latest
    autoremove: yes

- name: Install some applications
  apt:
    name: "{{ package }}"
    state: present
    update_cache: yes
