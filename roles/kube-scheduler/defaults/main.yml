---
# defaults file for kube-scheduler
scheduler:
  bindAddress: "127.0.0.1"
  extraArgs:
    - "--profiling=false"
    - "--port=0"
    - "--feature-gates=TTLAfterFinished=true"
    - "--logging-format=json"
  log:
    level: 1